namespace KeelPlugins
{
    internal static class TitleShortcutsConstants
    {
        public const string DESCRIPTION_AUTOSTART = "Choose which mode to start automatically when launching.\nDuring startup, " +
                                                     "hold esc or F1 to cancel automatic behaviour or hold another shortcut to use that instead.";
    }
}
